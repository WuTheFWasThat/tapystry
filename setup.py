from setuptools import setup

setup(
    name="tapestry",
    py_modules=["tapestry"],
    version="0.0.1",
    install_requires=[
    ],
    description="A redux-saga inspired effects library",
    author="Jeffrey Wu",
)
